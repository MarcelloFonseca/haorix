    import { ReactElement } from 'react';import * as React from "react"


        export default function Footer({css}: {css:string}): ReactElement {

            return (
                <footer className={css}>
                    //TODO: Terminer de faire le footer. Implementer un grid avec tailwind css
                </footer>
            )

        }